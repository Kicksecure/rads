# This is how rads works. It disables autostart of conventional display
# managers when installed, hooks into the boot process by using /etc/profile.d,
# and re-enables conventional display managers when uninstalled.
rads: postrm-contains-additional-updaterc.d-calls *
